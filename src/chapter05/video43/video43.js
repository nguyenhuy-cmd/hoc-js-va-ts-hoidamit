// Truy cập và cập nhập thuộc tính của Object

const person = {
    name: "Huy",
    age: 23
}

// Truy cập tuổi
console.log(person.age);

// Thêm thuộc tính
person.address = "Hà Nội"
console.log(address);

// Xóa thuộc tính
delete person.name